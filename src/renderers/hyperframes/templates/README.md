# HyperFrames Compositions

`WEBSITE_DEMO`、`LANDING_PAGE_DEMO` 和 `PRODUCT_PAGE_SCROLL` 会为每个 Scene 生成独立 HyperFrames 项目。

每个项目包含真实网页截图、本地 GSAP、本地字体、`DESIGN.md` 和 `index.html`，并依次通过 `lint`、`validate`、`inspect`、`render`。不存在占位渲染或跳过机制。

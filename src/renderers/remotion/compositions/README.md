# Remotion Compositions

MVP scene rendering is currently routed through `renderRemotionScene.ts` and FFmpeg so the full pipeline can run without blocking on visual polish.

Future real Remotion compositions should start here for:

- `HOOK`
- `SELLING_POINT`
- `CTA`

